import Layout from "@/components/layout";
import { ROLES } from "@/shared/constants";

const MedicalRecordsPage = () => {
    return(
        <>
        
        </>
    );
};

Profile.auth = {
    roles: [
        ROLES.PATIENT,
        ROLES.HEALTHCARE_PROVIDER
    ],
};

export default MedicalsRecordPage;